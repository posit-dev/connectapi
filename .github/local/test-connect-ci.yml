version: '2.3'
services:

  connect1:
    hostname: connect1
    image: rstudio/rstudio-connect:${CONNECT_VERSION}
    ports:
      - 3939
    privileged: true
    volumes:
      - "${RSC_LICENSE_FILE}:/var/lib/rstudio-connect/license.lic"
  connect2:
    hostname: connect2
    image: rstudio/rstudio-connect:${CONNECT_VERSION}
    ports:
      - 3939
    privileged: true
    volumes:
      - "${RSC_LICENSE_FILE}:/var/lib/rstudio-connect/license.lic"
